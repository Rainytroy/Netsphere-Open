# Netsphere 环境配置模板
# 重命名此文件为 .env 并填写您自己的值

# 基础设置
PORT=3001                     # 服务器监听端口
NODE_ENV=production           # 环境模式：development, production

# 数据库配置
DB_PATH=database/netsphere.db # SQLite数据库路径

# 安全设置
ENCRYPTION_KEY=               # API密钥加密密钥，必须设置为32个字符的强随机字符串
SESSION_SECRET=               # 会话密钥，必须设置为随机字符串

# 文件上传设置
UPLOAD_DIR=uploads            # 上传文件保存目录
MAX_UPLOAD_SIZE=5242880       # 最大上传大小(5MB)

# 跨域设置
CORS_ORIGIN=*                 # 允许的跨域源，生产环境中应修改为特定域名
